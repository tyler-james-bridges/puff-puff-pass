export const GAIN_LINES = [
  "{handle} sparked the rotation and moved to #{rank}",
  "{handle} is in chief mode at #{rank}",
  "Blunt passed to {handle}, now sitting at #{rank}",
  "{handle} climbed the smoke ladder to #{rank}",
  "{handle} took the session mic and landed at #{rank}",
  "{handle} is hotboxing the board at #{rank}",
  "{handle} lit up the lane and entered #{rank}",
  "{handle} elevated to #{rank} with a fresh pass",
  "{handle} rolled through and claimed #{rank}",
  "{handle} just floated into #{rank}"
];

export const LOSS_LINES = [
  "{handle} got ashed down to #{rank}",
  "{handle} lost the couch throne and dropped to #{rank}",
  "Rotation moved on, {handle} is now #{rank}",
  "{handle} got nudged to #{rank}, run it back",
  "{handle} slid to #{rank} after the latest pass",
  "{handle} took a hit to position, now #{rank}",
  "{handle} got bumped to #{rank}",
  "{handle} is now parked at #{rank}",
  "{handle} cooled off and fell to #{rank}",
  "{handle} got passed up and landed at #{rank}"
];

export const VIBE_TAGS = [
  "hotbox",
  "blunt-rotation",
  "chief-mode",
  "couchlock",
  "wake-n-bake"
];

